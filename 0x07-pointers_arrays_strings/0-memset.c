#include "main.h"

/**
 * _memset - function
 * @s: pointer to fill
 * @b: constant byte
 * @n: number to fill
 *
 * Return: return pointer
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		s[i] = b;
	}
	return (s);
}
